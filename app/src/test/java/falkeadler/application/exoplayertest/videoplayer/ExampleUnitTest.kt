package falkeadler.application.exoplayertest.videoplayer

import falkeadler.application.exoplayertest.videoplayer.services.ResponseMovie
import falkeadler.application.exoplayertest.videoplayer.services.ResponseSearchMovie
import falkeadler.application.exoplayertest.videoplayer.services.TMDBService
import org.junit.Test

import org.junit.Assert.*
import retrofit2.Call
import retrofit2.Callback
import retrofit2.Response
import java.net.URLEncoder

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}